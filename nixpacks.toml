[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'npm ci',
  'npm ci --prefix frontend',
  'npm ci --prefix backend'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'npm start'
