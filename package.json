{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npx cross-env FRONTEND_URL=http://localhost:3000 npm run -w backend dev",
    "dev:frontend": "npx cross-env NEXT_PUBLIC_BACKEND_URL=http://localhost:4000 npm run -w frontend dev",
    "build": "npm-run-all build:frontend build:backend",
    "build:frontend": "npm run -w frontend build",
    "build:backend": "npm run -w backend prisma:generate && npm run -w backend build",
    "start": "npm run -w backend start",
    "setup": "npm install"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.4"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
